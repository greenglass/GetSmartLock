apply plugin: 'java'

dependencies {
  testCompile 'junit:junit:4.12'
}

sourceSets {
  main {
    java {
      srcDir "src/main"
    }
  }

  test {
    java {
      srcDir "src/test"
    }
  }
}

jar {
  manifest {
    attributes "Main-Class": "com.getsmart.lock.GetSmartLock"
  }
}
